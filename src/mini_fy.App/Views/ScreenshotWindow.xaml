<Window x:Class="mini_fy.App.Views.ScreenshotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        WindowState="Maximized"
        Background="Transparent"
        Cursor="Cross"
        KeyDown="Window_KeyDown">
    <Grid x:Name="MainGrid">
        <!-- Screen freeze: full brightness -->
        <Image x:Name="BackgroundImage" Stretch="Fill" />

        <!-- Dark overlay with selection hole -->
        <Path x:Name="DimOverlay" Fill="#AA000000" Visibility="Collapsed" />

        <!-- Selection border (drawn on top of hole edge) -->
        <Rectangle x:Name="SelectionRect"
                   Stroke="#FF4080FF"
                   StrokeThickness="2"
                   Fill="Transparent"
                   Visibility="Collapsed" />

        <!-- Mouse interaction layer (topmost) -->
        <Canvas x:Name="SelectionCanvas" Background="Transparent"
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                MouseMove="Canvas_MouseMove"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp" />
    </Grid>
</Window>
