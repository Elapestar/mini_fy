<Window x:Class="mini_fy.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="mini_fy 设置" Height="620" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Hotkey -->
                <TextBlock Text="快捷键" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                <Border BorderBrush="#FFDDDDDD" BorderThickness="1" CornerRadius="4" Padding="10,8"
                        Background="#FFF8F8F8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="HotkeyDisplay" Text="Ctrl + Alt + Q"
                                   VerticalAlignment="Center" FontSize="14" FontFamily="Consolas" />
                        <Button Content="修改" Click="ChangeHotkey_Click"
                                Padding="10,4" Margin="16,0,0,0" />
                    </StackPanel>
                </Border>

                <!-- Screenshot mode -->
                <TextBlock Text="截图模式" FontWeight="Bold" FontSize="14" Margin="0,10,0,6"/>
                <StackPanel>
                    <RadioButton x:Name="ModeManualRadio" Content="手动框选"
                                 GroupName="TranslateMode" Margin="0,0,0,4" />
                    <RadioButton x:Name="ModeAutoRadio" Content="自动全屏翻译"
                                 GroupName="TranslateMode" Margin="0,0,0,4" />
                </StackPanel>

                <!-- Auto-close time (visible only in auto mode) -->
                <StackPanel x:Name="AutoClosePanel" Visibility="Collapsed" Margin="0,4,0,0">
                    <TextBlock Text="自动关闭时间 (秒)" FontSize="12" Foreground="#FF666666" Margin="0,0,0,2"/>
                    <TextBox x:Name="AutoCloseSecondsBox" Width="80" />
                </StackPanel>

                <Separator Margin="0,14" />

                <!-- Copy hotkey -->
                <TextBlock Text="复制译文快捷键" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                <Border BorderBrush="#FFDDDDDD" BorderThickness="1" CornerRadius="4" Padding="10,8"
                        Background="#FFF8F8F8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="CopyHotkeyDisplay" Text="Ctrl + Alt + S"
                                   VerticalAlignment="Center" FontSize="14" FontFamily="Consolas" />
                        <Button Content="修改" Click="ChangeCopyHotkey_Click"
                                Padding="10,4" Margin="16,0,0,0" />
                    </StackPanel>
                </Border>

                <Separator Margin="0,14" />

                <!-- Baidu API -->
                <TextBlock Text="百度翻译 API" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                <StackPanel>
                    <TextBlock Text="APPID" FontSize="12" Foreground="#FF666666" Margin="0,0,0,2"/>
                    <TextBox x:Name="AppIdTextBox" Margin="0,0,0,8" />
                    <TextBlock Text="API Key" FontSize="12" Foreground="#FF666666" Margin="0,0,0,2"/>
                    <PasswordBox x:Name="ApiKeyBox" Margin="0,0,0,8" />
                </StackPanel>

                <Separator Margin="0,14" />

                <!-- OCR Language -->
                <TextBlock Text="OCR 语言" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                <StackPanel>
                    <RadioButton x:Name="LangEnRadio" Content="英文 (English)" IsChecked="True"
                                 Margin="0,0,0,4" />
                    <RadioButton Content="自动检测 (后续版本支持)" IsEnabled="False"
                                 Margin="0,0,0,4" />
                </StackPanel>

                <Separator Margin="0,14" />

                <!-- Options -->
                <TextBlock Text="选项" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                <CheckBox x:Name="AutoSaveCheck" Content="截图后自动保存到本地"
                          Margin="0,0,0,6" />
                <CheckBox x:Name="AutoStartCheck" Content="开机自启"
                          Margin="0,0,0,6" />
                <CheckBox x:Name="AutoCopyCheck" Content="翻译结果自动复制到剪贴板"
                          IsChecked="True" Margin="0,0,0,6" />
                <CheckBox x:Name="BypassProxyCheck" Content="直连翻译 API（绕过系统代理）"
                          Margin="0,0,0,6" />

                <Separator Margin="0,14" />

                <!-- Maintenance -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <Button Content="清理缓存" Click="ClearCache_Click"
                            Padding="12,6" />
                    <Button Content="查看日志" Click="ViewLog_Click"
                            Padding="12,6" Margin="12,0,0,0" />
                </StackPanel>

                <Separator Margin="0,14" />

                <!-- Save/Cancel -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Click="Cancel_Click"
                            Padding="16,8" Width="80" IsCancel="True" />
                    <Button Content="保存" Click="Save_Click"
                            Padding="16,8" Width="80" Margin="12,0,0,0"
                            Background="#FF0078D4" Foreground="White" BorderThickness="0" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
